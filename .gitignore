# Runtime secrets — live only in /opt/portfolio-service/.env on the VPS.
.env
.env.*
!.env.example

# Build output
# Root-anchored: without the slash this would also swallow everything under
# cmd/portfolio-service/ (untracked new files, e.g. migrations).
/portfolio-service
